# atmega32a-hex-keypad-interface
4×4 hexadecimal keypad interfaced with ATmega32A using AVR Assembly. Key presses are detected and displayed on a 7-segment LED through keypad scanning and lookup tables.
